{
  "name": "reverse-image-search",
  "version": "2.1.0",
  "description": "Reverse image search - find image sources, dates, and similar images",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node -e \"try{require('puppeteer').executablePath()}catch(e){console.log('Chrome already handled')}\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^25.5.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "sharp": "^0.33.2"
  }
}
